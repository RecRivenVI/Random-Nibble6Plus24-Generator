package io.github.recrivenvi.randomnibble6plus24generator.worldgen.identity;

/** A dimension owns only its closed flag; the validated context belongs to its server. */
public interface MosaicLevelLifecycle {
    boolean randomnibble6plus24generator$identityLevelClosed();
}
